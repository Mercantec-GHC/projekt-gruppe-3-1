    @page "/profil_oplysninger"
@using BlazorApp.Service
@inject DBService DbService


            <!-- Vertical Pills Navigation -->
            <div class="d-flex align-items-start">
                <!-- Features Section -->
                <section id="features" class="container my-5">
                    <div class="row">
                        <div class="col-md-3">
                            <div class="card">
                                <div class="card-body">
                                    <img src="https://cbx-prod.b-cdn.net/COLOURBOX7139093.jpg" alt="Smiley face" width="42" height="42" class="rounded-circle" style="border:5px solid black">
                                    <h5 class="card-title">Navn</h5>

                                    <div class="nav flex-column nav-pills me-3" id="v-pills-tab" role="tablist" aria-orientation="vertical">
                                        <div class="list-group">
                                                <a href="/dashboard" class="bg-body-secondary list-group-item list-group-item-action">Dashboard</a>
                                                <a href="/profil_oplysninger" class="list-group-item list-group-item-action bg-secondary text-white active">Profil oplysninger</a>
                                                <a href="/mine_bilannoncer" class="bg-body-secondary list-group-item list-group-item-action">Mine Bilannoncer</a>
                                                <a href="/betalings_historik" class="bg-body-secondary list-group-item list-group-item-action">Betalings historik</a>
                                                <a href="/settings" class="bg-body-secondary list-group-item list-group-item-action">Settings</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">Biler liste</h5>
                                    <p class="card-text">Her kan du se alle data for dine oprettede salgsannoncer</p>
                                    <div class="tab-content" id="v-pills-tabContent">
                                        <div class="tab-pane fade show active" id="v-pills-home" role="tabpanel" aria-labelledby="v-pills-home-tab" tabindex="0">





<PageTitle>Brugere</PageTitle>

<h2 style="padding:  20px;">Brugere</h2>

<div class="container-fluid" style="background-color: #012169; padding: 20px;">
    @if (users == null)
    {
        <p>Loading...</p>
    }
    else if (!users.Any())
    {
        <p>Ingen brugere fundet.</p>
    }
    else
    {
        @foreach (var user in users)
        {
            <div class="card mb-3" style="max-width: 540px;">
                <div class="card-body">
                    <h5 class="card-title">@user.Name</h5>
                    <p class="card-text">Password: @user.Password</p>
                    <p class="card-text">Mobile: @user.Mobile</p>
                    <p class="card-text">Email: @user.Email</p>
                    <p class="card-text">City: @user.City</p>
                    <p class="card-text">Address: @user.Address</p>
                </div>
            </div>
        }
    }
</div>

@code {
    private List<UsersService.User>? users;

    protected override async Task OnInitializedAsync()
    {
        users = await DbService.GetAllUsersAsync(); 
    }
}




                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>